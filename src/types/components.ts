export type LetterT = {
  letter: string;
  show: boolean | undefined;
};

export type LoadingPropT = {
  loading: string | undefined;
};

export type ShowPropT = {
  show: boolean | undefined;
};

export type EndPropT = {
  end: boolean | undefined;
};

export interface LetterI {
  letter: LetterT;
}
